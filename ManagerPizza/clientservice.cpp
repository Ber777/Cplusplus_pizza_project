#include "clientservice.h"

ClientService::ClientService()
{

}

/*bool ClientService::Find(QString str)
{
    m_id = clientDataMapper.FindClient(str);
    if(m_id.isEmpty())
    {
        m_isInDataBase = false;
        return m_isInDataBase;
    }
    else
    {
        m_isInDataBase = true;
        return m_isInDataBase;
    }
}

QMap<int, QString> ClientService::SeeInfo()
{
    QMap<int, QString> map;
    map = clientDataMapper.SelectClient();
    return map;
}

bool ClientService::Delete(QString str)
{
    m_id = clientDataMapper.DeleteClient(str);
    if(m_id.isEmpty())
    {
        m_isInDataBase = false;
        return m_isInDataBase;
    }
    else
    {
        m_isInDataBase = true;
        return m_isInDataBase;
    }
}*/
